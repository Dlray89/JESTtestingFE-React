

type Project {
    id: ID! @id @unique
    projectName: String!
    description: String!
    createAt: DateTime! @createAt
}

type CheckList {
    id: ID! @id @unique
    itemName: String!
    completed: Boolean!
}